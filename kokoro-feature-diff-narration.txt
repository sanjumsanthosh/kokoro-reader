Phase zero. Repository files are src cache-queue TypeScript, cache-queue test TypeScript, main TypeScript, text TypeScript, text test TypeScript, Rust src lib, index HTML, and styles CSS. The webview owns interaction state; Rust owns project metadata, file deletion, and actual disk accounting. Before the cache panel exposed every audio clip; now it groups clips into one article row with the next incomplete clip. Before read state was true or false; now it records creation and read timestamps. Before right-click could not connect narration to source text; now it finds playback, selects narration, and selects the closest Markdown passage. A Project is one saved article, a Clip is one cached audio segment, and referenced cache size can overlap for shared files.

---

Change one. In src cache-queue TypeScript, line 47 export function summarizeCacheProjects receives tasks readonly CacheTask array. Line 52 creates prioritized with prioritizeCacheTasks tasks currentProjectId activeSection. Line 53 creates summaries Map string CacheProjectSummary. Lines 55 and 58 get summary by task projectId and create projectName, total, ready, queued, caching, failed, nextTask. Lines 63, 65, 66, and 67 increment summary total and summary task status, then replace nextTask when its status is ready and task status is not ready. This is the AFTER change: one summary per project rather than independent clip tasks. For Article A, two separate clip rows become one row, one of two cached and one pending; selecting clip two continues useful work.

---

Change two. In src main TypeScript, lines 573 and 579 are BEFORE: renderCacheTask list task and main innerHTML rendered one card per clip with repeated Section and Clip metadata. Line 608 is AFTER: renderCacheProject list summary. Line 610 derives state from summary caching, failed, ready equals total, and queued. Lines 615 and 617 build parts from summary ready total cached and summary queued pending. Line 620 calls navigateToCacheTask summary nextTask. Lines 626 and 628 set Retry failed and filter cacheQueue by task projectId, summary projectId, and task status failed before retryCacheTask task id. Line 641 sets cachePanelCount from complete and summaries length. The header changes from ready clips to complete articles; Article A reads one of two cached and one pending.

---

Change three. In Rust src lib, ProjectSummary has created_at u64 at line 120 and read_at Option u64 at line 123. ProjectMetadata has optional created_at and read_at at lines 130 and 131, so old metadata files deserialize. Lines 352 and 354 use metadata created_at unwrap_or file_timestamp document_path and metadata read_at. Lines 2708 and 2710 create ProjectMetadata and set read_at to previous read_at or Some epoch_seconds when read, else None. In main TypeScript, articleRows ProjectSummary filters articleFilter all Read Unread at line 1248, sorts articleSort created_at or left read_at at line 1250, and renderArticles at line 1259 renders selection, status, dates, cache bytes, actions. The table contains Date Added, Marked Read, Cached Audio, Open, Mark read, Mark unread, Delete, Delete selected, All, Unread, Read, newest added first, ascending, descending, and subdued read rows. Example Checkpoint: legacy article, Read Boolean only, filesystem-added date, unknown read date, sortable without inventing history; new read action, true plus read timestamp, completion date visible.

---

Change four. Rust line 1006 directory_size path Path Result u64 String measures physical files. Line 1011 metadata is_file returns metadata len. Line 1049 project_cache_usage document_dir cache_dir Result Vec ProjectCacheUsage reads cache references; lines 1055, 1059, and 1060 read_cache_references project, cached_audio_size cache_dir join format key wav, and bytes checked_add size. Line 1071 usage sort_by right bytes cmp left bytes. Line 1075 storage_stats_for_paths returns StorageStats, and line 1085 returns total_bytes, models, audio_cache, project_files, app_data, projects. Line 2584 storage_stats app exposes the Tauri report. It shows total physical usage, models, audio cache, project files, remaining app data, locations, Reveal in Finder, top five projects, human-readable size, cached clip count, read status, and shared WAV explanation. Example Checkpoint: cache file not reader-visible becomes 45 physical bytes and 1 article clip; shared file physical total once while references overlap.

---

Change five. Rust line 966 delete_stored_project is the existing single delete. At line 971 it delegates to delete_stored_projects document_dir cache_dir project_id string. Line 974 begins delete_stored_projects with project_ids string. Line 979 rejects project_ids is_empty with Select at least one project to delete. Lines 987 and 989 use BTreeSet seen insert project_id to reject Duplicate project ID in delete request. Line 993 calls ensure_project_exists before removal. Line 996 uses fs remove_dir_all after validation. Line 999 calls prune_orphaned_audio once after the final reference set. Lines 2738 and 2745 expose async delete_projects project_ids Vec String and reuse delete_stored_projects. This is batch deletion, validated targets, selected projects, project directory, orphaned audio, next available project, and one prune. Example Checkpoint: A and B selected, interface repeated single deletes, one validated batch, fewer intermediate states; audio references prune after each delete becomes prune once after A and B, keeping shared audio correctly.

---

Change six. In text TypeScript, line 268 export function speechChunkRanges source finds synthesized clip offsets. Line 272 uses source indexOf text searchFrom; line 275 returns text start exactStart end searchFrom. Line 281 export function bestMarkdownPassage source narration finds the closest paragraph. Line 288 uses narrationHighlights and line 289 returns matched block or whole source. In main TypeScript, contextEditTarget sectionIndex at line 1525 first uses currentPlayback projectId currentProjectId, then playbackPosition voice selectedVoice at 1528, then clicked section and clip zero at 1529. Lines 1506 and 1513 choose narrationRange from speechChunkRanges section speech_text editTarget clipIndex and passage from bestMarkdownPassage section markdown narrationRange text. Line 1537 Edit current passage sets editTarget and switches to Edit mode. Active clip two gets exact narration range and matching paragraph; no match gets whole current section, not fabricated precision.

---

Change seven. cache-queue test TypeScript line 52 tests collapses clips into article rows using the next incomplete task. Line 58 expects summaries map projectId to equal current other. Line 59 expects total two, ready one, queued one, nextTask id pending. text test TypeScript line 107 tests narration chunk offsets and the closest Markdown paragraph; lines 109 and 114 expect speechChunkRanges narration and bestMarkdownPassage source narration start nine end source length. Behavior flow: project documents create clip tasks; cache grouping produces article summary next incomplete clip; Rust metadata and storage commands provide dates and physical byte facts; Articles and Storage render facts; batch deletion safely returns to Rust; playback and text matching produce editor selections. Before: clip rows, Boolean-only read state, no storage report, no source jump. After: article rows, timestamps, physical storage totals, batch cleanup, narration-to-source editing. Run bun test, bun run build, cargo test manifest path src-tauri Cargo.toml. Observed: twenty four frontend tests, forty three Rust tests, and packaged macOS application build passed. Search, timer, narration-highlight changes were already in the dirty working tree and are not part of this feature walkthrough.
